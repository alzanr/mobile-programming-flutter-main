// Contoh penambahan di fungsi _onMenuTap:
else if (label == "IPK") {
  Navigator.push(
    context,
    MaterialPageRoute(builder: (context) => const IpkPage()), // Ganti IpkPage dengan nama class widget Anda
  );
}